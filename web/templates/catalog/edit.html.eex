<h2>Edit catalog</h2>

<%= render "form.html", changeset: @changeset,
                        action: catalog_path(@conn, :update, @catalog) %>

<%= link "Back", to: catalog_path(@conn, :show, @catalog.id) %>
